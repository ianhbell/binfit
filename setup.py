from distutils.core import setup
import setuptools

setup(name='binfit',
      version='0.0',
      description='A python tool for fitting binary interaction parameters for GERG-like models',
      author='Ian H. Bell',
      author_email='ian.bell@nist.gov',
      packages = ['.'],
      zip_safe = False
     )